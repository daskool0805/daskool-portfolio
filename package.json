{
  "name": "daskool-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Daskool graphic design portfolio \u2014 static export",
  "scripts": {
    "dev": "serve . -l 3000",
    "start": "serve . -l 3000",
    "build": "npm --prefix studio ci && npm --prefix studio run build && node scripts/prepare-deploy.mjs",
    "cms:dev": "npm --prefix studio run dev",
    "cms:migrate": "node scripts/migrate-projects.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  },
  "type": "module"
}
